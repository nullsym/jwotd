* Use env variables and make bot/website run on docker container

* Set up Failtoban

* Figure out gunicorn+prometheus

* Idea for bot? Ask for an example after the wotd, ask for strawpoll and add the most voted option
